#include <stdio.h>
#include <stdlib.h>

int main()
{
    // KITA MEMBUTUHKAN SEBUAH FORMAT SPECIFIER
    int favNum = 100;
    char myChar = 'i';
    printf("My favourite %c is %d", myChar, favNum );
    return 0;
}
